{% extends 'base.html.twig' %}

{% block title %}Vente de voiture{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<nav>
<h2>
<img src="{{asset('/image/logo.jpg')}}" alt="logo">
    <a class="container rounded bg-light link-dark link-underline-light"href="{{ path('listeProduit') }}">Liste des produits</a>
        <a class="container rounded bg-light link-dark link-underline-light" href="{{ path('app_login')}}"> espace admin</a>
        {% if app.user %}
            <div class="mb-3">
                Vous êtes connecté : {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}"class="container rounded bg-light link-dark link-underline-light">Déconnection</a>
            </div>
        {% endif %}
</h2>
</nav>
<div class="example-wrapper">
    <h1>Vente de voiture</h1>

    <h2> 5 derniers modèles</h2>
    {% for produit in liste_produit|reverse %}
     {% if loop.index <= 5 %}
<div class="container text-center">
<div class=" row  gap-3">
<div class="card col-6 p-2 align-self-stretch bg-light" style="width: 18rem;">
  <img src="{{ asset(produit.image) }}"class="card-img-top" alt="#" /></a>

  <div class="card-body">
    <h5 class="card-title">Titre = {{ produit.nom }}
</h5>
    <p class="card-text">Description = {{ produit.description }}
</p>
        <p>Stock = {{ produit.stock }} </p>

    <a href="{{ path('route_produit', {'id': (produit.id) }) }}" class="btn btn-dark">Détails produit : {{ produit.nom }}</a>
  </div>
</div>


          {% endif %}

     {% endfor %}
     </div>
</div>
</div>
{% endblock %}
